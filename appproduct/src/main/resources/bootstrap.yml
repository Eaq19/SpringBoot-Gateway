#Application Name
spring:
  application:
    name: product-services     # This is the name for the remote configuration file
  cloud:
    config:
      uri: http://localhost:9999  #where the config-service is running
      fail-fast: true             #the service will not run if it can't reach the config-service
      password: 
      username: 